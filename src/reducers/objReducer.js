const initState={
    id:1,
    fname:"abrar"
};

const objReducer =(state=initState, action)=>{
        switch(action.type)
        {
            
                default: return state;
        }
        

}

export default objReducer;
